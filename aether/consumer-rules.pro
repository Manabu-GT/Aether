# Aether library requires no consumer ProGuard rules.
# All dispatch is compile-time traceable (interface polymorphism, sealed class matching).
# R8 correctly handles dead code elimination for unused effects.
